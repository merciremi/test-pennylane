<section>
  <h1>Our best recipes with <%= @ingredients.join %></h1>
  <hr>
</section>

<section>
  <div>
    <% @recipes.each do |recipe| %>
      <%= render recipe %>
    <% end %>
  </div>
  <div>
    <%= paginate @recipes %>
  </div>
</section>
